# Arena backend environment.
# REQUIRED for the Phase 2 secret store. Generate a key with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ARENA_SECRET_KEY=
# Where the encrypted secret DB lives (default: data/secrets.db)
ARENA_SECRETS_DB=data/secrets.db